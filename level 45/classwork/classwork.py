# 1) შექმენით სია სადაც შეინახავთ სახელებს,თქვენი დავალებაა რომ სიიდან ამოშალთ მხოლოდ ის ელემენტები რომლის სიგრძე 
# მეტია 5 ზე გამოიყენეთ remove() და for i in ... <--- სიის სახელი,გადაუარეთ პირდაპირ სიას რეინჯის გარეშე,დაპრინტე სია 
# და ნახეთ ამოიშალა თუ არა ის ელემენტები რომლის სიგრძეც მეტი იყო 5 ზე

names = ["jonathan", "brian", "james", "ray", "reginald"]

for i in range(len(names)-1, -1, -1):
    if len(names[i]) > 5:
        names.remove(names[i])
print(names)